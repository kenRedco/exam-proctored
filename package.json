{
  "name": "topproctor",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo Use: npm run dev -w apps/api | npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "start": "npm run start -w apps/api & npm run start -w apps/web",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "lint": "echo Add lint config or use eslint later"
  },
  "dependencies": {
    "mongodb": "^6.19.0"
  }
}
